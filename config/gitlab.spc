connection "gitlab" {
  plugin = "theapsgroup/gitlab"

  # GitLab personal access token
  token = "glpat-xxxx"

  # Self-hosted GitLab (omit for gitlab.com)
  baseurl = "https://your-gitlab-server.com"
}
