# Steampipe Bundle Configuration
#
# Copy this file to steampipe.env and customize:
#   cp steampipe.env.example steampipe.env
#   vi steampipe.env
#

# Database port for PostgreSQL connections
STEAMPIPE_DATABASE_PORT=9193

# Log level: trace, debug, info, warn, error
STEAMPIPE_LOG_LEVEL=warn

# Listen mode: local (localhost only) or network (all interfaces)
# STEAMPIPE_LISTEN=network

# Note: Plugin configs (.spc files) go in the config/ directory
# Copy templates: cp config-templates/*.spc config/
