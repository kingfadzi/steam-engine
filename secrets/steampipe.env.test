# Steampipe test configuration
# Mock credentials for CI - allows startup, API calls will fail
#
# For real testing, copy actual credentials to secrets/steampipe.env

# Service settings
STEAMPIPE_DATABASE_LISTEN=network
STEAMPIPE_DATABASE_PORT=9193
STEAMPIPE_LOG_LEVEL=WARN

# Jira credentials (mock)
JIRA_URL=http://mock-jira:8080
JIRA_TOKEN=test-token-not-real

# GitLab credentials (mock)
GITLAB_URL=https://mock-gitlab
GITLAB_TOKEN=glpat-test-not-real

# Bitbucket credentials (mock)
BITBUCKET_URL=https://mock-bitbucket:7990
BITBUCKET_USERNAME=test-user
BITBUCKET_PASSWORD=test-token-not-real
