# Gateway test configuration
# Test DW connection for CI
#
# For real testing, copy actual credentials to secrets/gateway.env

# Data Warehouse connection (test-dw service)
DW_HOST=test-dw
DW_PORT=5432
DW_DATABASE=test_db
DW_USER=gateway
DW_PASSWORD=testpassword

# Disable sync in test mode
SYNC_ENABLED=false
